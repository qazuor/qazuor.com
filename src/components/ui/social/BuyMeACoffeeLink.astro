---
import SocialLink from './SocialLink.astro';

interface Props {
    href: string;
    ariaLabel?: string;
    size?: 'sm' | 'md' | 'lg';
    className?: string;
}

const { href, ariaLabel = 'Buy Me a Coffee', size = 'md', className = '' } = Astro.props;
---

<SocialLink href={href} ariaLabel={ariaLabel} iconName="buymeacoffee" size={size} className={className} />
