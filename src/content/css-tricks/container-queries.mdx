---
title: "Container Queries"
description: "Components that respond to their container's size, not the viewport. Perfect for reusable UI."
tags: ["css", "responsive", "container-queries", "layout"]
featured: true
browserSupport: ["Chrome 105+", "Firefox 110+", "Safari 16+"]
createdAt: 2025-11-27T00:00:00Z
order: 1
---

import { CSSPlayground } from '@/components/interactive/CSSPlayground';

# Container Queries

Make components respond to their container size instead of the viewport.

```css
.card-container {
  container-type: inline-size;
}

@container (min-width: 400px) {
  .card {
    grid-template-columns: 200px 1fr;
  }
}
```

## Live Demo

<CSSPlayground
  client:load
  controls={[
    { id: "breakpoint", type: "range", label: "Breakpoint", min: 200, max: 400, default: 300, unit: "px" }
  ]}
  cssTemplate={`.cq-container {
  container-type: inline-size;
  width: 100%;
}
.cq-card {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1rem;
  padding: 1rem;
  background: rgba(139, 92, 246, 0.1);
  border-radius: 0.5rem;
}
.cq-icon {
  width: 60px;
  height: 60px;
  background: linear-gradient(135deg, #8b5cf6, #06b6d4);
  border-radius: 0.5rem;
  flex-shrink: 0;
}
.cq-title {
  margin: 0 0 0.25rem 0;
  font-size: 1rem;
  font-weight: 600;
}
.cq-desc {
  margin: 0;
  font-size: 0.875rem;
  opacity: 0.7;
}
@container (min-width: {{breakpoint}}) {
  .cq-card {
    grid-template-columns: 60px 1fr;
    align-items: center;
  }
}`}
  previewHTML={`<div class="cq-container">
  <div class="cq-card">
    <div class="cq-icon"></div>
    <div>
      <h4 class="cq-title">Card Title</h4>
      <p class="cq-desc">Layout changes based on container width.</p>
    </div>
  </div>
</div>`}
  title="Container Queries"
  badge="RESIZE"
  previewMinHeight="140px"
  resizable={true}
/>

Drag the right edge of the preview to see the card layout switch between stacked and horizontal based on the container width.
