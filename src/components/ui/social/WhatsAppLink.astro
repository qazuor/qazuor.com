---
import SocialLink from './SocialLink.astro';

interface Props {
    href: string;
    ariaLabel?: string;
    size?: 'sm' | 'md' | 'lg';
    className?: string;
}

const { href, ariaLabel = 'WhatsApp', size = 'md', className = '' } = Astro.props;
---

<SocialLink href={href} ariaLabel={ariaLabel} iconName="whatsapp" size={size} className={className} />
